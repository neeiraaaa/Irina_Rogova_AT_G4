package tasks.homework.stringTask;

public class isGranted {

    public boolean isGranted(boolean isGranted) {
        if (!isGranted) {
            return false;
        } else
        return true;
    }
}

